# Codex 知识库协作说明

## 仓库定位

本目录既是 Obsidian Vault，也是 Git 仓库。它用于长期积累视觉传达设计学习、课程与作品集项目、软件技能、灵感素材、就业准备、其他兴趣主题，以及用户主动要求保存的个人成长记录。

主要使用中文，文件统一采用 UTF-8 Markdown。目标是让内容清楚、可检索、可关联、可长期回看，而不是只堆放原始资料。

## 开始工作前

1. 先阅读 `README.md`，了解目录结构和知识库使用方式。
2. 所有任务都遵循 `AI助手规范.md`；本文件只作为入口，不重复其中的完整规则。
3. 涉及个性化建议、长期目标或近期状态时，再读取 `个人成长档案.md`。其中“待补充”或“待确认”的内容不得当作事实。
4. 用户说“我要写日记”“记录一下今天”或同义表达时，必须先读取并遵循 `日记记录规范.md`，先采访、后写入，不凭空补全。
5. 处理某个主题前，优先查看对应目录的 `README.md` 和已有笔记，避免重复创建内容。

用户本次明确提出的要求优先于本文件中的一般工作习惯。遇到冲突、信息不足或可能造成不可恢复影响时，先说明情况并请求确认。

## 目录与归档

- `00_收件箱`：新收到、尚未整理的资料。
- `01_设计基础`：色彩、构成、字体、版式、设计史等基础知识。
- `02_软件技能`：Photoshop、Illustrator、Figma 等工具学习。
- `03_设计案例`：品牌、海报、包装、UI 等案例分析。
- `04_我的项目`：课程作业、练习、作品集项目和复盘。
- `05_设计行业与就业`：岗位、公司、作品集和求职准备。
- `06_灵感素材`：配色、字体、图片、网页和视觉参考。
- `07_其他主题`：设计以外但需要长期积累的主题。
- `90_待整理`：暂时无法可靠分类或仍需用户确认的内容。
- `99_归档`：已经过时但需要保留的内容。
- `.private`：只保存在本机的敏感资料，不提交、不上传、不在普通笔记中复制其完整内容。

每日资讯由外部本机项目 `E:\Horizon` 负责抓取和摘要，结果同步到 `07_其他主题/每日资讯/Horizon`。不要在本知识库内重复创建同一项定时抓取任务。

## Obsidian 兼容规则

- 优先使用标准 Markdown 标题、列表、表格和代码块，保证离开 Obsidian 后仍可阅读。
- 关联知识库内已有笔记时，可使用 Obsidian 双向链接 `[[笔记名]]`；显示名称不清楚时使用 `[[笔记名|显示文字]]`。
- 不随意改动已有文件名或路径，因为这可能破坏 Obsidian 链接。确需移动或重命名时，先检查引用并保留可追溯记录。
- 新笔记的文件名应清楚表达主题；日期型记录使用 `YYYY-MM-DD`。
- 不为了格式统一而批量改写旧笔记，也不擅自修改 `.obsidian`、插件或主题配置。

## Codex 工作方式

- 回答问题时先使用知识库中的已有内容，并指出参考了哪些笔记；知识库没有记录时应明确说明。
- 整理资料时保留来源、作者、日期和原始路径；把“资料原意”“用户的理解”和“AI 的总结或建议”分开。
- 不编造用户经历、来源、文件、日期或结论，不把单次情绪写成长期人格特征或医学诊断。
- 未经明确要求，不删除、覆盖、移动或重命名原始资料，不进行大范围重构。
- 新资料无法可靠分类时放入 `90_待整理`，并列出需要用户确认的问题。
- 修改前先检查相关文件；修改后复查变更范围，运行 `git diff --check`，并用 `git status --short` 确认没有误改无关文件。
- 未经用户明确要求，不执行 `git commit`、`git push`，也不把私人记录发送到任何外部服务。

## 隐私边界

- 不写入密码、API 密钥、身份证号、精确住址、完整聊天记录或不必要的第三方个人信息。
- 班级名单、学号、联系方式等内容只能保存在 `.private`，不得提交到远程仓库。
- 个人档案和日记只在用户主动要求时更新；日记中的单日状态不会自动同步为长期档案结论。
- 需要联网核实资料时，只传递完成检索所需的最少信息，不上传个人文件或私人内容。
